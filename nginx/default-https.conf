server {
    listen 80;
    server_name www.levgorev.com levgorev.com;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://www.levgorev.com$request_uri;
    }
}

server {
    listen 443 ssl;
    server_name www.levgorev.com;

    ssl_certificate /etc/letsencrypt/live/www.levgorev.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/www.levgorev.com/privkey.pem;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;

    root /var/www/html;
    index index.html;

    location /static/ {
        expires 7d;
        add_header Cache-Control "public, immutable";
    }

    location /api/ {
        proxy_pass http://backend:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }

    gzip on;
    gzip_types text/plain text/css application/json application/javascript;
}

server {
    listen 443 ssl;
    server_name levgorev.com;
    ssl_certificate /etc/letsencrypt/live/www.levgorev.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/www.levgorev.com/privkey.pem;
    return 301 https://www.levgorev.com$request_uri;
}
